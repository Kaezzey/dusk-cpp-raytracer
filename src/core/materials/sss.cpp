#include "../../include/core/materials/sss.h"

// The header contains all functions as inline helpers for now. This
// cpp file exists so the build system can compile a translation unit
// if desired and to make it easy to extend with non-inline helpers
// later.
